export type OrganizationSettingsFormValues = {
  name: string;
  email: string;
  phone: string;
  domain: string;
  timezone: string;
  language: string;
  currency: string;
};

export type ProfileSettingsFormValues = {
  firstName: string;
  lastName: string;
  phone: string;
};
